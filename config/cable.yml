development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://notcord.herokuapp.com/cable" } %>  #CHECK url: <%= ENV['REDIS_URL'] %>
  channel_prefix: notcord_production
