development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>  #CHECK url: <%= ENV['REDIS_URL'] %>
  channel_prefix: notcord_production
